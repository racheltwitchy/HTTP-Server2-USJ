package http.server.usj;

import java.util.*;

public class CarAPI {
    List<Car> Carlist;
    /*
     * List<Car> getCars();
     * Car getCarById(int id);
     * void addCar(Car car);
     * void updateCar(int id, Car car);
     * void deleteCar(int id);
     */
}
